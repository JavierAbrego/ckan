/target

# Los paquetes .deb son artefactos de build, no se versionan.
*.deb

# Notas de trabajo del manager: estado, tareas y decisiones. Son internas y
# estan en espanol; el repo publico no las necesita.
/.manager
